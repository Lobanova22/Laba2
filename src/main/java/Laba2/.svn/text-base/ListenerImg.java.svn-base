package Laba2;

import java.awt.event.*;

import javax.swing.JLabel;


public class ListenerImg implements ActionListener {
	
	public void actionPerformed(ActionEvent e) {
	
		subversion.image = new Image();
		subversion.main_panel.add(subversion.image);//добавление на главную панель
		subversion.image.setSize(500,250);// установка размеров
		subversion.image.setLocation(270,15);
		subversion.image.setVisible(true); // установка видимости
	
	}
	}
	

